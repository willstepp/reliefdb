<center>
  <%=RedCloth.new(@page).to_html%>
  <%=button_to "Edit This Page", {:action => "edit"},:class => 'buttonleft' if session['user'] && User.find(session['user']) && User.find(session['user']).priv_admin%>

